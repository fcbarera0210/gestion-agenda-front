---
import Layout from '../../layouts/Layout.astro';
import AgendarPage from '../../components/AgendarPage';

const { professionalId } = Astro.params;

export function getStaticPaths() {
  return [];
}
---

<Layout title="Agendar">
  <AgendarPage client:load professionalId={professionalId} />
</Layout>
